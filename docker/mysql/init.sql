-- MySQL initialization for Laravel
-- Note: Database name, username, and password are set via environment variables
-- in docker-compose.yml (MYSQL_DATABASE, MYSQL_USER, MYSQL_PASSWORD)

-- The database and user are automatically created by MySQL Docker image
-- using the environment variables. This file is kept for any additional
-- initialization that might be needed in the future.

-- Example of additional initialization (uncomment if needed):
-- CREATE TABLE IF NOT EXISTS `example_table` (
--     `id` INT AUTO_INCREMENT PRIMARY KEY,
--     `name` VARCHAR(255) NOT NULL,
--     `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP
-- );
